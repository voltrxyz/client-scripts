import { DRIFT } from "./drift";
import { KLEND } from "./klend";
import { MARGINFI } from "./marginfi";
import { SOLEND } from "./solend";

export const PROTOCOL_CONSTANTS = {
  SOLEND,
  MARGINFI,
  DRIFT,
  KLEND,
};
